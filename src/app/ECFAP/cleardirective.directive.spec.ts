import { CleardirectiveDirective } from './cleardirective.directive';

describe('CleardirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CleardirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
