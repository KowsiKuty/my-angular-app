import { ApptodecimalDirective } from './apptodecimal.directive';

describe('ApptodecimalDirective', () => {
  it('should create an instance', () => {
    const directive = new ApptodecimalDirective();
    expect(directive).toBeTruthy();
  });
});
