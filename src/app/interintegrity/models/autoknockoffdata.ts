export interface Autoknockoffdata {
    gl_date: Date,
    bs_date: Date,
    gl_runningbalance: number,
    bs_runningbalance: number,
    difference: number,
    bs_accountno: string,
    gl_accountno: string,
    gl_branchcode: string
}
