import { AmountNotificationDirective } from './amount-notification.directive';

describe('AmountNotificationDirective', () => {
  it('should create an instance', () => {
    const directive = new AmountNotificationDirective();
    expect(directive).toBeTruthy();
  });
});
